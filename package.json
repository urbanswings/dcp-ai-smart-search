{
  "name": "dcp-ai-smart-search",
  "version": "1.1.0",
  "main": "index.js",
  "scripts": {
    "test": "TEST_MODE=both TMPDIR=.playwright-tmp PLAYWRIGHT_CACHE_DIR=.playwright-cache npx playwright test && node generate-results-html.js",    
    "test:ui-only": "TEST_MODE=ui TMPDIR=.playwright-tmp PLAYWRIGHT_CACHE_DIR=.playwright-cache npx playwright test && node generate-results-html.js",
    "test:api-only": "TEST_MODE=api TMPDIR=.playwright-tmp PLAYWRIGHT_CACHE_DIR=.playwright-cache npx playwright test && node generate-results-html.js",
    "report": "node generate-results-html.js && open $(ls -t results/html/*.html | head -1)",
    "clean": "./clean-old-results.sh",
    "convert-facets": "node tests/utils/facetToJson.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/axios": "^0.9.36",
    "axios": "^1.12.2",
    "dotenv": "^17.2.3",
    "openai": "^6.0.1"
  }
}
